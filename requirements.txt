# ion_gym dependencies
# Core (always needed):
numpy
scipy
numba
# Interactive app + plotting:
panel
plotly
bokeh
# Memory telemetry (ion_gym/ui/telemetry.py). NOT optional in practice:
# without psutil, rss_mb() falls back to resource.ru_maxrss, which is the
# HIGH-WATER MARK and cannot fall. On a session that frees memory the
# heartbeat then reports a number that is simply wrong, and a whole
# debugging session was spent acting on it (2026-09-14).
psutil
# STL / CAD electrode path (build_stl, voxelize) — the quadrupole example
# and any STL-backed geometry. trimesh reads/voxelizes meshes; manifold3d
# is trimesh's boolean backend, needed for bored plates (einzel-STL
# example). Inline-shape examples (planar/round einzel, funnel) do NOT
# need these.
trimesh
manifold3d

# Geometry editor (ion_gym/edit): DC contour overlay. contourpy arrives
# implicitly with matplotlib today; declared explicitly here so the
# editor's dependency does not ride on matplotlib's internals
# (merge of the editor branch, v416).
contourpy

# notebooks (demonstration + V-series + code atlas)
matplotlib
cma
nbformat
jupyter_bokeh
